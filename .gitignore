# Node / Vite
node_modules/
dist/
.vite/

# Project-local build staging
.build/

# Rust / Cargo
app/target/
**/*.rs.bk
**/*.pdb

# Tauri sidecar 二进制（cli:build 产物，按平台生成）
app/flowix-desktop/binaries/

# Dev logs（运行 tauri dev / vite 产生）
tauri-dev.log
tauri-dev.err.log
vite-dev.log
vite-dev.err.log

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*~

# Env
.env
.env.local
.env.*.local
app/flowix-desktop/tauri.conf.production.local.json

# Supabase CLI local state
supabase/.temp/

# Build outputs
*.tsbuildinfo

# Local runtime / scratch files should live under .build/runtime/.
# Keep legacy path ignores below so old tools cannot pollute the worktree.

# Local Codex / coding-agent session logs (developer telemetry; never ship)
test.json

# Legacy local runtime / scratch paths
momo-yellow.png
scratch-build.log
verify-icon.ps1
tmp/

# 本地专属文件（不进远端）
CLAUDE.md
