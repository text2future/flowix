# 常用命令

> 命令速查。**这里没有的命令，不要自创**——先确认是否有既定做法。

## 一、日常开发

| 目的 | 命令 |
| --- | --- |
| 安装依赖 | `<cmd>` |
| 启动开发 | `<cmd>` |
| 构建 | `<cmd>` |

## 二、质量检查

| 目的 | 命令 |
| --- | --- |
| 类型检查 | `<cmd>` |
| Lint / 格式化 | `<cmd>` |
| 单元测试 | `<cmd>` |
| 全部检查 | `<cmd>` |

> **提交前必须跑**"全部检查"。见 `开发者入口/测试与检查命令.md`。

## 三、数据

| 目的 | 命令 |
| --- | --- |
| 初始化 / 重置 | `<cmd>` |
| 生成合成数据 | `<cmd>` |
| 执行迁移 | `<cmd>` |
| 回滚迁移 | `<cmd>` |

## 四、发布与运维

| 目的 | 命令 |
| --- | --- |
| 打包 | `<cmd>` |
| 冒烟测试 | `<cmd>` |
| 查看日志 | `<cmd>` |

## 五、维护规则

- 新增命令 → 登记本表（含一句话说明）。
- 命令废弃 → 标注替代者，不要直接删。
- 命令与 `package.json`（或等价清单）不一致 → 以本表为准并修正代码。
