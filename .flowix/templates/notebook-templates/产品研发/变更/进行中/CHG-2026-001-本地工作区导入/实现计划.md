# CHG-2026-001 · 实现计划

> **怎么改**：拆成可独立验证的步骤，每步说清"改哪、怎么算做完"。
> 任务跟踪见 `任务与进度.md`。

## 一、总体思路

```
选择目录 → 校验范围 → 分批扫描 → 写入索引 → 完成回调
              └─ 可中断 / 可恢复 ─┘
```

## 二、步骤

| # | 步骤 | 涉及模块 | 完成判据 |
| --- | --- | --- | --- |
| 1 | 目录选择与范围校验 | 桌面端能力 | 能正确拒绝越界路径 |
| 2 | 分批扫描 + 上限控制 | 主进程 / 索引 | 大目录内存不超阈 |
| 3 | 索引写入（含中文分词） | 索引 | 中文可被检索到 |
| 4 | 进度上报与中断 | 前端 + 主进程 | 中断后状态一致 |
| 5 | 契约与文档同步 | 契约层 | 端点 / 模型已登记 |

## 三、契约影响

| 契约 | 改动 | 兼容性 |
| --- | --- | --- |
| `<API 端点>` | 新增 | 兼容 |
| `<数据表>` | 新增列 | 兼容（可空） |
| `<IPC 通道>` | 新增 | 兼容 |

## 四、明确不做（防止范围蔓延）

- 重构无关模块
- 顺手改索引结构（**单独变更**）
- 增加实时文件监听
