# 技术栈与本地环境

> **本文件回答**：用了什么技术、什么版本、本机怎么跑起来。
> 与 `开发者入口/开发环境准备.md` 的分工：本文件是**事实清单**，那份是**操作步骤**。

## 一、技术栈

| 层 | 技术 | 版本 | 为什么选它（若有 ADR 则引用） |
| --- | --- | --- | --- |
| 语言 | <...> | | |
| 框架 | | | |
| 数据库 / 存储 | | | |
| 构建 / 打包 | | | |
| 测试 | | | |

> 版本**以 lock 文件为准**，本表只登记"我们期望的主版本"，避免频繁过期。

## 二、运行时要求

| 项 | 要求 |
| --- | --- |
| 运行时版本 | <>= X.Y> |
| 包管理器 | <名称 + 版本> |
| 操作系统 | <是否跨平台；支持哪些> |
| 其他依赖 | <如原生工具链> |

## 三、环境变量

| 名称 | 用途 | 本地默认 |
| --- | --- | --- |
| `<NAME>` | <用途> | <默认> |

> 真值来源见 `发布与运行/配置与密钥管理.md`；**密钥不进本文件**。

## 四、平台差异速查

见 `模块与契约/桌面端与本地能力/跨平台差异.md`。

## 五、维护规则

- 技术栈变化 → 更新本表 + 必要时写 ADR（重大选型）。
- 版本升级 → 见 `架构决策/依赖升级计划.md`。
- 环境要求变化 → 同步 `开发者入口/开发环境准备.md`。
