# 代码地图

> **本文件回答**：代码在哪、从哪读起。
> **性质**：事实描述。代码结构变化后**必须**回写（由 `代码与规格收敛` 负责）。

## 一、目录结构

```
<按实际仓库填写>

src/
├── <层1>/          # <一句话职责>
├── <层2>/
└── ...
tests/
migrations/
```

> 只写到"能定位"的粒度即可，**不要**逐文件罗列（会迅速过期）。

## 二、读代码的入口

| 想了解 | 从哪读 |
| --- | --- |
| 应用如何启动 | <入口文件> |
| 路由 / 端点定义 | <位置> |
| 数据访问 | <位置> |
| 配置读取 | <位置> |
| 后台任务 | <位置> |

## 三、关键文件

| 文件 | 作用 | 改动注意 |
| --- | --- | --- |
| <路径> | <作用> | <为什么危险 / 需要谁评审> |

## 四、如何保持不过期

| 触发 | 动作 |
| --- | --- |
| 新增顶层目录 | 更新第一节 |
| 移动 / 重命名核心文件 | 更新第二、三节 |
| 每次变更收敛 | 由 `代码与规格收敛` 核对本文件 |

> **判定标准**：陌生人照本文件能否在 5 分钟内找到目标代码。做不到就是过期了。
