# 运行架构

> **本文件回答**：跑起来之后，进程 / 服务 / 数据是怎么流动的。
> 与 `架构决策/架构演进路线.md` 的分工：本文件描述**现状**，那份描述**意图**。

## 一、拓扑

```
<按实际情况画。至少包含：进程 / 服务、数据存储、关键数据流向。>
```

## 二、进程与职责

| 进程 / 服务 | 职责 | 启动方式 | 可多实例 |
| --- | --- | --- | --- |
| <名称> | <职责> | <命令 / 系统管理> | 是 / 否 |

## 三、关键数据流

| 场景 | 流向 | 说明 |
| --- | --- | --- |
| <例如：导入> | <入口 → 处理 → 存储> | <同步 / 异步；失败如何处理> |

> 只画**关键链路**（能反映系统形态的那几条），不要罗列所有调用。

## 四、状态与生命周期

| 项 | 说明 |
| --- | --- |
| 有状态处 | <哪里存了状态、怎么恢复> |
| 启动时序 | <谁必须先起来> |
| 优雅退出 | <如何处理进行中的任务> |
| 崩溃恢复 | <重启后如何自愈> |

## 五、维护规则

- 拓扑变化 → 更新本文件，并评估是否触发 `架构决策/架构演进路线.md` 的阶段变化。
- 由 `代码与规格收敛` 在变更收尾时核对。
