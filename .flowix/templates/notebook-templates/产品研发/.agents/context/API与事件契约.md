# API 与事件契约（索引）

> **本文件是索引，不是权威。** 权威在 `模块与契约/API/` 与 `模块与契约/后端/任务与事件.md`。
> 存在意义：让代理/开发者**一处就能看到"有哪些契约"，然后跳转**。

## 一、对外接口

| 端点 | 用途 | 认证 | 权威文件 |
| --- | --- | --- | --- |
| `<METHOD /path>` | <用途> | 是 | `模块与契约/API/API目录.md` |

## 二、内部事件

| 事件 | 触发 | 消费方 | 权威文件 |
| --- | --- | --- | --- |
| `<event.name>` | <何时> | <谁> | `模块与契约/后端/任务与事件.md` |

## 三、外部集成

| 集成 | 方向 | 权威文件 |
| --- | --- | --- |
| <第三方> | 出 / 入 | `模块与契约/API/Webhook与外部集成.md` |

## 四、契约变更检查

| # | 检查项 |
| --- | --- |
| 1 | 权威文件已更新（**先改文档再改代码**） |
| 2 | 兼容性已判定（`模块与契约/API/版本与兼容性.md`） |
| 3 | 破坏性变更已有过渡方案 + ADR |
| 4 | 本索引已同步 |
| 5 | 消费方已通知（内部 + 外部） |

## 五、维护规则

- 新增 / 下线契约 → 更新本索引 + 权威文件。
- 本索引与权威文件不一致 → 以权威文件为准，并修正索引。
