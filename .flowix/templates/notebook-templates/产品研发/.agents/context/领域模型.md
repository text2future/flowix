# 领域模型

> **本文件回答**：业务上有哪些核心概念、它们什么关系。**与代码实现无关，只讲业务语义。**

## 一、核心概念

| 概念 | 含义 | 关键属性 | 归属 |
| --- | --- | --- | --- |
| <概念> | <一句话业务含义> | <属性> | <模块> |

> **关键**：概念名称**必须**全库统一。同一事物在不同地方用不同词，是 bug 的温床（登记到 `术语表`，见第二节）。

## 二、术语对照

| 业务术语 | 代码 / 表名 | 说明 |
| --- | --- | --- |
| <术语> | `<code>` | <避免歧义的解释> |

## 三、关系

```
<概念A> 1 ── n <概念B>
```

| 关系 | 基数 | 业务含义 |
| --- | --- | --- |
| A → B | 1:n | <业务上意味着什么> |

## 四、业务规则（不变量）

| # | 规则 | 违反意味着 |
| --- | --- | --- |
| 1 | <例如：某状态只能从 A 到 B> | <数据不一致 / 业务错误> |

> 这些不变量**必须**在代码中有对应校验，且**必须**有测试。

## 五、填写范例

| 概念 | 含义 | 关键属性 |
| --- | --- | --- |
| 工作区 | 一个被纳管的本地目录 | 路径、状态 |

## 六、维护规则

- 新增业务概念 → 更新本文件 + 同步 `模块与契约/数据/数据模型.md`。
- 术语变更 → **全库统一改**，禁止新旧混用。
- 领域规则变化 → 评估影响面，可能需 ADR。
