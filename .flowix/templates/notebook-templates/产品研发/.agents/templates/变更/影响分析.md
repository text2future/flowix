<!--
模板：影响分析
用途：动手前的"体检报告"——改哪里、影响谁、有什么风险。
位置：变更/<状态>/CHG-YYYY-NNN-<slug>/影响分析.md
由技能产出：代码库理解与影响分析
-->

# CHG-YYYY-NNN · 影响分析

| 项 | 内容 |
| --- | --- |
| 变更号 | CHG-YYYY-NNN |
| 分析人 | <姓名 / AI> |
| 日期 | YYYY-MM-DD |
| 读过的上下文 | `.agents/context/`：<列出> · `.agents/rules/`：<列出> |

## 一、需求理解

<用自己的话复述需求，确认理解正确。>

## 二、入口与调用链

**入口**：<路由 / 命令 / 事件 / API handler>

```
<入口>
  └─► <模块 A>.method()
        └─► <模块 B>.method()
              └─► <数据 / 外部依赖>
```

## 三、影响面（六类）

### 3.1 代码

| 文件 / 模块 | 改动类型 | 说明 |
| --- | --- | --- |
| <路径> | 修改 / 新增 / 只读但要回归 | <说明> |

### 3.2 契约

| 契约 | 是否变更 | 兼容性 |
| --- | --- | --- |
| API <端点> | 是 / 否 | 兼容 / 破坏（需版本或过渡） |
| 事件 <名称> | 是 / 否 | 兼容 / 破坏 |
| 共享类型 | 是 / 否 | — |

### 3.3 数据

| 项 | 说明 |
| --- | --- |
| Schema 变更 | 有 / 无（有 → 需 migrate + rollback） |
| 数据迁移 / 回填 | 有 / 无（有 → 分批 + 幂等 + 可中止） |
| 可逆性 | 可逆 / **不可逆（需用户确认）** |

### 3.4 用户可见行为

<默认值、UI、交互、文案、权限是否有变化。>

### 3.5 依赖

| 依赖 | 变化 | 风险 |
| --- | --- | --- |
| <库 / 服务> | 新增 / 升级 / 移除 | <风险> |

### 3.6 运行

<配置、环境变量、资源用量、权限、部署单元是否变化。>

## 四、风险清单

| # | 风险 | 等级 | 理由 | 应对 |
| --- | --- | --- | --- | --- |
| R1 | <风险> | 高 / 中 / 低 | <理由> | <应对> |

## 五、待确认问题

> **不要猜**。不确定的都写在这里。

- [ ] <问题> —— 需问：<产品 / 架构 / 用户>

## 六、建议实现范围

**最小可行改动**：<范围>

**建议顺序**：<粗粒度步骤>

## 七、上下文修正记录（如发现）

| 发现 | 实际 | 处理 |
| --- | --- | --- |
| `.agents/context/<文件>` 与代码不符 | <真实情况> | 已修正 / 待修正 |

---

**下一步**：`变更实现计划` → 产出 `实现计划.md`、`任务与进度.md`
